import { createBrowserRouter } from 'react-router-dom'

export const router = createBrowserRouter([
  {
    path: '/',
    element: <div>Backlog Page (placeholder)</div>,
  },
  {
    path: '/sprint',
    element: <div>Sprint Page (placeholder)</div>,
  },
])
